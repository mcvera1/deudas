spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/deudas-personales
    username: root
    password: nacional
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true

  flyway:
    baseline-on-migrate: true